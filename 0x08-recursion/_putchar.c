#include "main.h"
#include <unistd.h>
/**
 * _putchar - writes a character on standard output (screen)
 * @c: characer which will be printed.
 * Return: 0
 */
int _putchar(char c)
{
	return (0, &c, 0);
}
